version: '3'
services:
  db: 
    image: postgres:16
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: ${password}
      POSTGRES_DB: ${name}
    container_name: agrosoft_db
    volumes:
      - ./postgres:/var/lib/postgresql/data